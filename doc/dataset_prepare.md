# 📊 Описание алгоритма создания датасета изображений

> 💡 Данный алгоритм реализован в файле [`utils/dataset_prepare.py`](../utils/dataset_prepare.py) и предназначен для подготовки датасета изображений с применением различных аугментаций и созданием аннотаций в формате Pascal VOC.

## 🧩 Основные этапы алгоритма

### 🔧 Инициализация и настройка параметров
   - Импорт необходимых библиотек (OpenCV, NumPy, random и др.)
   - Настройка логирования
   - Определение функций для различных видов обработки изображений

### 🛠️ Функции обработки изображений

#### 🌫️ Размытие изображения (`blur_image`)
   - С вероятностью `blur_prob` применяется гауссово размытие
   - Размер ядра размытия увеличивается случайным образом

#### 📊 Добавление шумов (`noise_image`)
   - С вероятностью `noise_prob` добавляется гауссов шум
   - Уровень шума определяется случайным образом

#### 🔄 Поворот изображения (`rotation_image`)
   - С вероятностью `rotation_prob` изображение поворачивается на случайный угол от -5 до +5 градусов
   - Автоматически вычисляется новый размер изображения после поворота

#### 📏 Нормирование разрешения (`resize_image`)
   - Если разрешение изображения меньше минимального (3000×2000), оно увеличивается
   - Если разрешение превышает максимальное (6000×4000), оно уменьшается
   - Сохраняется пропорциональность изображения

#### 🧨 Добавление дефектов (`defects_image`)
   - С вероятностью `defects_prob` добавляются различные дефекты
   - С вероятностью `balance` добавляются либо пятна, либо царапины
   - Для пятен: создаются эллипсы случайного размера, цвета и ориентации с полупрозрачностью
   - Для царапин: рисуются белые линии случайной длины и толщины
   - Автоматически генерируются bounding box и полигональные аннотации для каждого дефекта

3. **Основной процесс подготовки датасета (`dataset_prepare`)**

   - Создается выходная директория, если она не существует
   - Для каждого изображения из исходного датасета:
     1. Загружается исходное изображение
     2. Создается базовое нормализованное изображение
     3. Применяются последовательно: размытие, шум, поворот и добавление дефектов
     4. Генерируется аннотация в формате Pascal VOC с описанием всех добавленных дефектов
     5. Сохраняется обработанное изображение и соответствующий XML-файл аннотации
     6. С вероятностью `double_prob` создаются дополнительные дубликаты с разными параметрами аугментации

4. **Формат выходных данных**

   - Обработанные изображения сохраняются в формате JPG с именами вида `roll{номер}-{дубликат}.jpg`
   - Для каждого изображения создается XML-файл аннотации в формате Pascal VOC с тем же базовым именем
   - В аннотациях содержится:
     - Общая информация об изображении (размер, имя файла)
     - Для каждого дефекта: тип ("ellipse" или "scratch"), bounding box и полигон

5. **Параметры командной строки**

   Алгоритм может быть запущен с параметрами:
   - `--dataset_path`: путь к исходному датасету (по умолчанию "dataset/")
   - `--dataset_save_path`: путь для сохранения обработанного датасета (по умолчанию "prep_dataset/")
   - `--blur_prob`: вероятность применения размытия (по умолчанию 0.2)
   - `--noise_prob`: вероятность добавления шума (по умолчанию 0.2)